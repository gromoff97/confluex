# Confluex Test Migration Matrix

| old test class | selftest case | requirement ids | deletion status |
| --- | --- | --- | --- |
| CLI parsing, help, and validation unit tests in `tests/node/cli-*.test.js` | selftest command-surface regression validates supported commands, required options, rejected legacy options, and diagnostics before command work | FR-0007, FR-0008, FR-CONF | retained until selftest has byte-shape help assertions and all parser precedence cases |
| Env-file and config unit tests in `tests/node/config-*.test.js` | selftest env-file precedence case covers default `.confluex.env`, explicit `--env-file`, CLI override, process env fallback, and missing required values | FR-CONF | retained until selftest covers process/env-file/CLI precedence without local mocks |
| Remote auth and doctor tests in `tests/node/remote-access.test.js` and `tests/node/doctor-command.test.js` | selftest token-auth diagnostics covers valid token, invalid token, missing token, inaccessible page, transport classification, and token redaction | FR-CONF, FR-RUN | retained until live selftest exercises token-only REST and diagnostic reason classes |
| Markdown/export/report tests in `tests/node/*export*.test.js`, `tests/node/*report*.test.js`, and `tests/live-bats/02-confluex-live-commands.bats` | selftest export regression covers Markdown payloads, reports, scope findings, attachments, ZIP artifact, and golden snapshot comparison | FR-RUN | retained until selftest covers ZIP plus the current golden report contract |
| Fixture bundle and support preflight tests in `tests/node/selftest-*.test.js` | selftest support preflight validates `tests/fixtures/confluence-7137` assets before fixture application | FR-BASE, FR-RUN | retained as selftest implementation unit coverage |
| Install, uninstall, runtime footprint, and safety tests in `tests/node/*install*.test.js`, `tests/node/runtime-footprint.test.js`, and `tests/live-bats/09-config-install-security-resume.bats` | selftest lifecycle smoke covers install/update/uninstall behavior and manifest-owned footprint | FR-LIFE | retained until npm package install model replaces internal install semantics |
| Bats live regression entrypoint in `tests/live-bats/live-regression.bats` and helpers | selftest live regression currently delegates to Bats for end-to-end stand coverage | FR-RUN | retained until selftest no longer shells out to Bats |
